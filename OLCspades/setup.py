from setuptools import setup, find_packages

setup(
    name='olctools',
    version='0.1.0',
    description='Things used in the assembly pipeline in package format.',
    packages=find_packages('.')
)
